<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Your Cart</title>
    <link rel="stylesheet" href="styles.css">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="index.php">Fortnite Guide</a>
            <ul class="navbar-nav">
                <li class="nav-item"><a class="nav-link" href="shop.php">Shop</a></li>
                <li class="nav-item"><a class="nav-link" href="cart.php">Your Cart</a></li>
            </ul>
        </nav>
    </header>

    <main class="container mt-4">
        <h2>Your Cart</h2>

        {% if cart_empty %}
            <p>Your cart is empty. Go back to the <a href="shop.php">shop</a> to add some skins!</p>
        {% else %}
            <div class="row">
                {% for item in cart_items %}
                    <div class="col-md-4">
                        <div class="card">
                            <img src="{{ item.image_url }}" alt="{{ item.name }}" class="card-img-top">
                            <div class="card-body">
                                <h5 class="card-title">{{ item.name }}</h5>
                                <p class="card-text">Price: {{ item.price }} V-Bucks</p>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <h4>Total Price: {{ total_price }} V-Bucks</h4>

            <!-- Clear Cart Button (Form for POST request) -->
            <form method="POST" action="cart.php" class="mt-3">
                <button type="submit" name="clear_cart" class="btn btn-warning">Clear Cart</button>
            </form>
        {% endif %}
    </main>

    <footer>
        <p>&copy; 2024 Fortnite Guide. All rights reserved.</p>
    </footer>

    <!-- Optional JavaScript -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>